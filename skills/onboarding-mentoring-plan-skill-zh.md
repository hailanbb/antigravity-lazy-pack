---
name: onboarding-mentoring-plan-skill-zh-v1
description: 新员工入职带教 Skill，用于把真实业务资料转成结构化结果、行动清单和复核事项。
---

# 新员工入职带教 Skill

## 何时使用
当用户需要处理“新员工入职安排、岗位学习计划、带教任务拆解、试用期跟进”相关任务时使用本 Skill。

## 输入材料
- **原始业务资料**：聊天记录、表格、录音转写、照片说明或文档片段。
- **业务规则**：判断标准、责任边界、风险等级和输出口径。
- **输出要求**：需要给谁看、用于决策还是执行、是否需要复核。

## 工作流
1. 先识别资料类型，清理重复、缺失和无关信息。
2. 按模板抽取关键字段，形成结构化中间表。
3. 使用规则库判断优先级、风险点、责任人和下一步动作。
4. 输出可直接使用的结果，并列出待人工确认事项。
5. 如果信息不足，先提问补齐，不要编造。

## 输出要求
- 必须包含摘要、关键证据、风险等级、行动项、责任人、截止时间和待复核事项。
- 不能替用户做不可逆承诺，涉及金额、录用、处罚、合规结论时必须标注“需人工确认”。
- 输出应使用简体中文，避免空泛建议。

---

## 参考资料与模板 (References & Templates)

### 1. 入职带教规则 (references/rules.md)
```markdown
# 入职带教规则

- 每个阶段必须有具体产出物。
- 不得一次性堆砌过多资料，优先给最小必要路径。
- 涉及制度、薪酬、绩效的解释必须提醒以公司正式文件为准。
```

### 2. 入职带教计划模板 (templates/output_template.md)
```markdown
# 入职带教计划模板

## 一、岗位目标
- 岗位：
- 30 天目标：

## 二、阶段任务
| 阶段 | 学习内容 | 实操任务 | 导师检查点 | 验收标准 |
|---|---|---|---|---|

## 三、风险提醒
- 可能卡点：
- 需要主管介入：
```

### 3. 资源说明 (assets/README.md)
```markdown
这里可放置示例截图、字段说明图或业务模板。
```

---

## 脚本参考 (Scripts)

### 1. 数据清洗脚本 (scripts/normalize_input.py)
```python
#!/usr/bin/env python3
# 简单示例：将多行文本整理为去空行列表
import sys
text = sys.stdin.read()
lines = [x.strip() for x in text.splitlines() if x.strip()]
print('\n'.join(lines))
```
